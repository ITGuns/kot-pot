import type { SeedModifierGroup } from "./types";

/**
 * The source menu lists no modifiers / add-ons, so none are seeded.
 * Staff can create option groups under Admin → Menu → Modifiers and attach
 * them to items from the item editor.
 */
export const MODIFIER_GROUPS: SeedModifierGroup[] = [];
